
server:
  port: 8761

eureka:
  server:
    eviction-interval-timer-in-ms: 60000
#  instance:
#    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
#    serviceUrl:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/apps

logging:
  level:
    com.netflix.eureka: OFF
    com.netflix.discovery: OFF
spring:
  application:
    name: service-registry
